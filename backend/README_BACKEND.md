# Backend - FastAPI

Run backend:

1. Create virtual environment.
2. Install dependencies from backend/requirements.txt.
3. Copy backend/.env.example to backend/.env and set keys.
4. Start API:

uvicorn app.main:app --reload --app-dir backend

Main docs:
- Swagger UI: /docs
- ReDoc: /redoc
